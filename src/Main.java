import java.util.ArrayList;

public class Main {
    public static void main(String[] args) {
        // проверку оставила для второго ревью по заданию, если так нельзя делать, то удалю

        /* Manager manager = new Manager();
        ArrayList a = new ArrayList<>();
        Task newTask = new Task();
        manager.createTask(newTask);
        Epic newEpic = new Epic();
        manager.createEpic(newEpic);
        Subtask newSub = new Subtask();
        Subtask newSub1 = new Subtask();
        manager.createSubTask(newSub);
        manager.createSubTask(newSub1);
        System.out.println(manager.findAllTasks());
        System.out.println(manager.findAllEpics());
        System.out.println(manager.findAllSubtasks());

        //System.out.println(manager.deleteTask());
        //System.out.println(manager.deleteSubtask());
        //System.out.println(manager.findAllTasks());
        System.out.println(manager.findAllSubtasks());

        System.out.println(manager.showSubtaskById(2));

        manager.createTask(newTask);
        Task newTask1 = new Task();
        manager.updateEpic(newEpic);
        System.out.println(manager.findAllEpics());

        manager.deleteByIdTask(3);
        System.out.println(manager.getSubtaskOfEpic(1));
     */
    }
}